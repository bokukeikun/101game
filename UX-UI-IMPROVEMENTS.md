# 101 ゲーム — UX/UI 改善提案

最終更新: 2026-06-17

このドキュメントは、オンラインカードゲーム「101」全体の UX/UI を向上させるための提案をまとめたものです。
依頼された **マスト対応 2 件** と、現状のゲーム機能を踏まえた **追加提案** を、優先度・効果・工数の観点で整理します。

---

## 0. 現状のゲーム機能の理解（前提整理）

改善案の根拠を明確にするため、まず現状の仕様を整理します。

### ゲームの流れ

1. **ホーム**（`Homepage.vue`）: 名前を入力して `CREATE GAME` / `JOIN GAME`。ルームコードは 5 文字。招待リンク（`/?roomCode=XXXXX`）でも参加可能。最大 6 人。
2. **待機**（`Waiting.vue`）: ホストが `START`。各プレイヤーに 3 枚配り、残りは山札（`drawCardPile`）。最初のターンはランダム。
3. **プレイ**（`Play.vue` / `MiddleInfo.vue` / `PlayerList.vue`）: 手番のプレイヤーがカードを出し、`totalNumber`（合計）を **101 を超えないように** 加減算していく。
4. **脱落 / 勝敗**: 合計が 101 を超えるカードしか出せない＝ミス（`missPlayer`）→ `FOLD` で脱落、`ranking` に記録。最後の 1 人が勝者。
5. **ランキング**（`Ranking.vue`）: 結果表示。ホストは `RESTART` / `QUIT`。

### カードの種類（`utils/packOfCards.ts`）

| 記号 | 効果 | 枚数 |
|------|------|------|
| `N01`〜`N10` | 合計に加算 | 各 3〜6 枚 |
| `N50` | +50 | 2 |
| `M01` / `M10` | −1 / −10 | 2 / 4 |
| `Pas` | パス（手番スキップ） | 4 |
| `Trn` | 進行方向の反転（`isReturn`） | 4 |
| `Dbl` | 次の効果を倍化（`double`） | 2 |
| `Hnd` | 合計を一気に 101 にする | 5 |

### 同期・状態管理

- Firestore の 2 ドキュメントを `onSnapshot` で購読: `users/{roomCode}`（参加者）、`initGameState/{roomCode}`（ゲーム状態）。
- Pinia: `room`（参加者・ホスト判定）、`game`（盤面状態）、`user`。
- ホストは名前先頭に `H`、参加者は `C`。
- i18n（`vue-i18n`、ja デフォルト / en）。設定は右上ギアアイコン（`LanguageSettings.vue`、現状は **言語切替のみ**）。

### 現状で見えている UX 上の弱点（サマリ）

- カード画像に **白い枠**が焼き込まれていてフェルト背景で浮く（→ マスト①）。
- ギア設定が言語切替だけで、**ゲーム中の操作（中断・退出）がメニューに無い**（→ マスト②）。
- **遊び方・カード効果の説明が画面内に一切ない**（`Trn`/`Dbl`/`Hnd` 等は初見では不明）。
- **合計値（101 までの現在値）が常時表示されない**（`Check Total` を押すと 2 秒だけ表示）。これはゲームの最重要情報。
- **手番のタイムアウトが無い**（放置されると進行が止まり、ホストが手動削除するしかない）。
- 進行方向の反転（`Trn`）・倍化（`Dbl`）の **状態が UI に出ない**。
- 相手の **残り手札枚数が見えない**（戦略情報の欠落）。
- コピー完了が `alert()`、`isMyTurn` が TODO のまま等の細かな粗。

---

## 1. 【マスト①】カード画像の白い枠の改善

### 原因（調査結果）

`src/assets/images/cards-front/*.png`（全 21 種）と `card-back.png` は、
**色付きの角丸カードの外側に「不透明な白い枠（余白）」が画像そのものに焼き込まれている**ことを確認しました。
緑のフェルト背景に置くと、この白枠が縁取り（ハロー）として見えてしまいます。

> 検証: 透明な角の外側に不透明白 → 緑背景上で白フレームとして可視化。
> CSS の `border-radius` は画像の外周（白枠を含む長方形）を丸めるだけなので、**CSS だけでは白枠は消えません**（`border-radius` 除去でも変化なし）。

### 対応案

#### 案 A（推奨）: アセット側で白枠を透明化

カード内部の数字・記号も白いため、単純な「白 → 透明」置換は内側の白も消してしまいます。
そこで **画像の四隅（外周）から連結している白だけを透明化（エッジシードのフラッドフィル）** します。内部の白いグリフは保持されます。

- ツール例: ImageMagick の `-fuzz N% -fill none -draw "color 0,0 floodfill"`（四隅から実行）、または `sharp` / `pngjs` を使った小スクリプトで一括処理（全 22 枚）。
- 仕上げ: 白枠を消した後、CSS で **本物のカードらしい影**を付与（`box-shadow`）し、立体感を出す。

```scss
.card {
  border-radius: 8px;            // 画像の角丸に合わせる
  box-shadow: 0 2px 6px rgba(0, 0, 0, 0.35);
  // object-fit: contain; は維持
}
```

- メリット: 根本解決。全画面（手札・場・山札）で綺麗になる。
- 注意: 元画像のバックアップを取り、フラッドフィルの `fuzz` 値を 1〜2 枚で目視確認してから一括適用。

#### 案 B（暫定 / アセット非改変）: 画像を拡大してクリップ

白枠が一定幅の余白なので、コンテナで `overflow: hidden` し、画像を少し拡大して白枠をクリップ枠の外へ押し出します。

```scss
.card-frame { overflow: hidden; border-radius: 8px; }
.card-frame img { transform: scale(1.12); } // 白枠を外へ
```

- メリット: 画像を触らず即対応可能。
- デメリット: 端の絵柄が僅かに切れる／角丸が画像と二重になる可能性。**案 A までのつなぎ**として位置づけ。

#### 影響範囲

- `src/views/Play.vue` … `.card`（手札）
- `src/components/organisms/MiddleInfo.vue` … `.played-card`（場のカード）
- 山札・カード裏（`card-back.png`）を表示する箇所があれば同様に。

> CSS を変更するため、**完了前に `yarn dev` + DevTools 375px で手札・場のカードを目視確認**（AGENTS.md ルール）。

---

## 2. 【マスト②】設定ボタンからゲームを強制終了する機能

### 現状

- ギア設定（`LanguageSettings.vue`）は **App 全体に常設**（`App.vue`）され、言語切替のみ。
- ゲーム中断は、左上ロゴ → モーダル（`TopInfo.vue`）の「ホームに戻る」からのみ。設定メニューには無い。

### 設計方針

設定パネルに **「ゲーム」セクション**を追加し、**ゲーム中のみ**表示する。役割で出し分ける。

| 役割 | メニュー項目 | 挙動 |
|------|-------------|------|
| ホスト | **ゲームを終了**（全員終了） | 確認 → `users`/`restartUsers` を空に、`initGameState`・`users` ドキュメント削除 → 全員が `RoomClosed` へ |
| 参加者 | **ゲームから退出** | 確認 → 自分を `users`/`restartUsers` から除外 → ホーム（再参加可能）へ |

### 実装のポイント

1. **表示条件**: 設定パネルにゲーム用セクションを追加し、`roomStore.roomCode` が有効、かつ現在ルートが `game` のときだけ表示（`useRoute().name === 'game'`）。
2. **誤操作防止**: 強制終了は破壊的操作なので **確認ダイアログ**（`Modal.vue` 流用）を必ず挟む。文言は i18n 化。
3. **ロジックの共通化（推奨）**: 「ホスト終了 / 参加者退出 / 次ターン算出」のロジックが現状 `Game.vue`・`TopInfo.vue`・`Ranking.vue`・`MiddleInfo.vue` に重複している。
   `composables/useGameActions.ts` に `quitAsHost()` / `leaveAsClient()` を切り出し、設定パネルと各画面で再利用する。これにより設定からの強制終了と既存導線の挙動が完全に一致する。
4. **i18n 追加例**: `settings.gameSection`（"ゲーム"）, `settings.endGame`（"ゲームを終了"）, `settings.leaveGame`（"ゲームから退出"）, 確認文言。

### UI 案

ギアを開いたパネルに「言語」セクションの下へ区切り線を入れ、「ゲーム」セクションを追加。
強制終了ボタンは赤系（`.game-button.red` 相当）で危険操作と分かるようにする。

---

## 3. 追加提案（現状機能を踏まえた “あると良い” 機能）

優先度: ★★★ = 体験への影響大 / ★★ = 中 / ★ = 余裕があれば。

### ★★★ A. 合計値（101 メーター）の常時表示

現状、ゲームの核心である **現在の合計 / 101** が `Check Total` を押した 2 秒間しか見えない。
→ 画面中央上部に **常時表示のメーター/プログレスバー**（例: `73 / 101`、残り `28`）を置く。101 に近づくほど色を黄→赤に変化させ緊張感を演出。
直近に出されたカードの差分（`+10`, `-10` 等）をポップアップ表示すると因果が分かりやすい。

### ★★★ B. 遊び方・カード効果ガイド

`Trn`（反転）/`Dbl`（倍化）/`Hnd`（101 にする）/`Pas`/`M-`（マイナス）/`N50` は初見では不明。
→ 設定パネルまたは待機画面に **「遊び方」モーダル**を追加。各カードのアイコン＋効果を一覧表示。
最低限、各カードを **長押し／ホバーでツールチップ**表示でも効果は大きい。

### ★★★ C. 手番タイムアウト（自動パス／スキップ）

放置プレイヤーがいると進行が止まる（現状はホストの手動削除のみ）。
→ 手番に **カウントダウン（例: 30 秒）** を表示し、時間切れで自動パス or 自動フォールド。
先ほど改善した「ターン中チップ」にリング型のカウントダウンを重ねると自然。

### ★★ D. 相手の残り手札枚数の表示

プレイヤーリストの各チップに **残り枚数バッジ**（`playerDecks[name].length`）を表示。
誰が上がり間近かが分かり戦略性が増す。山札の残り枚数も表示すると良い。

### ★★ E. 進行方向・倍化などの状態インジケータ

`Trn` による反転（`isReturn`）、`Dbl`（`double>1`）が UI に出ていない。
→ 中央に **方向矢印（時計回り/反時計回り）** と、倍化中は **「×2」バッジ**を表示。状態変化時にアニメーション。

### ★★ F. フィードバック強化（音・触覚・トースト）

- 自分の手番開始時に **効果音 + バイブ（`navigator.vibrate`）**。
- カード再生・ミス時の効果音とアニメーション（ミスは既存 `Warning` のポップを活用）。
- `alert()`（招待コピー）を **非ブロッキングなトースト**に置換（`waiting.copied`）。
- 音はミュート可能にし、設定に **サウンド ON/OFF** を追加。

### ★★ G. オンライン/接続状態の表示

Firestore の presence（`onDisconnect` 相当の仕組み）で、**各プレイヤーのオンライン状態**をチップに表示。
切断者が分かり、ホストの「削除」判断がしやすくなる。

### ★ H. ランキングの演出強化

勝者の表彰（紙吹雪・1 位ハイライト・アニメーション）、自分の順位ハイライト、`RESTART` 待ちの体験改善。

### ★ I. アクセシビリティ / 入力体験

- 色のみに依存しない状態表現（アイコン併用）。
- 削除ボタン等のタップ領域確保（44px 目安）。
- カードに `alt` で効果名を入れる（読み上げ対応）。

### ★ J. PWA / 共有体験

- ルーム招待を **Web Share API**（`navigator.share`）でネイティブ共有。
- 既に `manifest.json` があるため、**ホーム追加プロンプト**やオフライン時の案内を整備。

---

## 4. 技術的負債・リファクタ提案（UX を安定させる土台）

- **ターン計算ロジックの重複排除**: `nextTurn`/`returnNextTurn` の同一コードが `Play.vue`・`MiddleInfo.vue`・`TopInfo.vue`・`PlayerList.vue` に散在。`utils/turn.ts` に集約。
- **終了/退出処理の共通化**: 上記マスト②の `useGameActions` で統一。
- **`game.ts` の `isMyTurn` が TODO のまま**: `currentUser` と `turn` を比較する実装に（各画面の `turn === currentUser` 判定を集約）。
- **ハードコード文言の i18n 化**: 追加 UI も含め文言は locale ファイルへ。

---

## 5. 優先順位まとめ

| # | 項目 | 区分 | 効果 | 工数 | 推奨着手 |
|---|------|------|------|------|----------|
| 1 | カード白枠の除去（案 A） | マスト | ★★★ | 中 | すぐ |
| 2 | 設定からゲーム強制終了/退出 | マスト | ★★★ | 中 | すぐ |
| A | 101 メーター常時表示 | 追加 | ★★★ | 中 | 早期 |
| B | 遊び方・カード効果ガイド | 追加 | ★★★ | 小〜中 | 早期 |
| C | 手番タイムアウト | 追加 | ★★★ | 中 | 早期 |
| D | 相手の残り手札枚数 | 追加 | ★★ | 小 | 中期 |
| E | 方向/倍化インジケータ | 追加 | ★★ | 小 | 中期 |
| F | 音・触覚・トースト | 追加 | ★★ | 中 | 中期 |
| G | オンライン状態表示 | 追加 | ★★ | 中 | 中期 |
| H | ランキング演出 | 追加 | ★ | 小 | 後期 |
| I | アクセシビリティ | 追加 | ★ | 小 | 随時 |
| J | PWA/共有 | 追加 | ★ | 小 | 後期 |
| — | ロジック共通化リファクタ | 基盤 | — | 中 | マスト②と同時 |

---

## 6. 進め方の提案

1. **マスト①（白枠）** … アセット一括処理 + CSS 影付け。`yarn dev` の 375px で確認。
2. **マスト②（強制終了）** … `useGameActions` 切り出し → 設定パネルにゲームセクション + 確認ダイアログ。
3. その後、**101 メーター → 遊び方ガイド → 手番タイマー** の順で着手すると体験の伸びが大きい。

> 実装に進める項目が決まれば、該当ファイルへの具体的な変更まで対応します。
